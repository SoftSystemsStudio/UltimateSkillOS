# UltimateSkillOS Requirements
# Python 3.12+

# Core ML/AI Dependencies
torch==2.9.1
transformers==4.57.1
sentence-transformers==5.1.2
faiss-cpu==1.12.0
numpy==2.3.4
huggingface-hub==0.36.0

# Data Validation & Schemas
pydantic>=2.0.0
pydantic-core>=2.0.0

# Configuration Management
tomli>=2.0.0  # TOML parser (built-in for Python 3.11+)
pyyaml>=6.0  # YAML support (optional)

# Optional Dependencies (for extended features)
# tavily==1.0.0  # Uncomment for web research capability
redis>=4.0.0  # Optional: Redis client for circuit-breaker persistence (set SKILLOS_CIRCUIT_REDIS_URL)

# Development & Testing
pytest>=7.0.0
black>=23.0.0
isort>=5.0.0
pylance>=0.0.1

# Note: Install with: pip install -r requirements.txt
# Or with optional features: pip install -r requirements.txt tavily
